import { SpecialityfilterPipe } from './specialityfilter.pipe';

describe('SpecialityfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SpecialityfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
